import { Audiometry, Patient } from '@/db/db';

export const exportToDocx = async (exam: Audiometry, patient: Patient) => {
  alert('Exportação para Word está temporariamente desativada devido à atualização da estrutura do exame.');
};
